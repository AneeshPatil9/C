#include <stdio.h>
int main() 
{
    int c[5]={0,0,0,0,0};
    int r,i,p;
    for(p=1;p<=5;p++)
    {
        printf("Enter your rating from 1-5");
        scanf("%d",&r);
        c[r-1]++;
    }
    for(i=0;i<5;i++)
        printf("\n Count of rating %d display:%d",i+1,c[i]);

    return 0;
}
